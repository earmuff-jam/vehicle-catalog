import { Stack, Typography } from "@mui/material";

const CenterText = () => {
  return (
    <Stack sx={{ textAlign: "center" }}>
      <Typography>Enter your VIN and model year below.</Typography>
    </Stack>
  );
};

export default CenterText;
